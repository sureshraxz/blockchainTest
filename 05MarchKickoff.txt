1. cmd used to view the list of commits in local repository?
git show
git log
git diff
git pull

2.stagging all the changes you have in your local repo

git commit .
git commit --add-all
git add -all
git add .

3.what happens to an untracked file when the command 'git add<file name> is executed?

the file moves to tracked.
the file gets commited.
the file moves to the staging area
nothing happens

4.cmd to see all the changes since last commit 

git see changes
git status
git view changes

5.I have made commit and discarded it.I can still retrieve that commit using 
'git reflog' -->T/F